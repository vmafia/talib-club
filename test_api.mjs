import handler from './api/reserve-book-campaign.js'; import * as fbAdmin from './api/_firebase-admin.js'; fbAdmin.verifyIdToken = async () => ({ uid: 'test-user-123' }); const req = { method: 'POST', body: JSON.stringify({ campaignId: 'c1' }), headers: { authorization: 'Bearer dummy' } }; const res = { setHeader: () => {}, status: (s) => ({ json: (d) => console.log('STATUS:', s, 'DATA:', d) }) }; handler(req, res).catch(console.error);